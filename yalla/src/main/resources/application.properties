spring.application.name=yalla

# JWT Configuration
app.jwt.issuer=yalla-api
app.jwt.audience=yalla-clients
app.jwt.secret=${JWT_SECRET:Uh0WvRj5qs2sChP0dwTkky/VoudWSk0FVikEDDUKdRU=}
app.jwt.ttl-minutes=60

# API Client credentials (for machine-to-machine authorization)
app.api.client-id=yalla-mobile-app
app.api.client-secret=changeme

# H2 Database
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# JPA
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=create-drop

# Password Reset
app.password-reset.token-validity-minutes=10

# Email Configuration
# Set app.email.enabled=true and configure SMTP to enable real email sending
app.email.enabled=${EMAIL_ENABLED:true}
app.email.from=${EMAIL_FROM:noreply@yallauni.com}

# Gmail SMTP Configuration
# To use Gmail, you need an "App Password" (not your regular password)
# 1. Enable 2FA on your Google account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate an app password for "Mail"
# 4. Use that 16-character password below
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:ioannidisgiorgos22@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:gubk ifqi xevo nsjd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000